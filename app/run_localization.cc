#include "common/basetype.h"

string config_yaml = "./config/mapping.yaml";

int main(int argc, char** argv)
{
    cout << "app start" << endl;

    cout << "app end" << endl;
    while(1);
    return 0;
}